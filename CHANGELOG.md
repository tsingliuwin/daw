# Changelog

本项目遵循 [Keep a Changelog](https://keepachangelog.com/zh-CN/1.1.0/) 格式，版本号遵循[语义化版本](https://semver.org/lang/zh-CN/)。

## [0.1.8] - 2026-08-27

### 新增

- **选表经验知识类别（selections/）**：数据分析 Agent 选表前先查阅已沉淀的「选表经验」——哪类业务问题用哪些表（首选表、交叉验证表、慎用/排除）。命中直接按经验选表、跳过探索；分析中确立的表选择偏好（用户纠正、验证过的映射）也会立即沉淀，下次同类问题不再重复探索。知识库大纲与提示词同步纳入该类别。
- **list_remote_tables 注释/行数/filter**：探查数据源时 postgres 表现在附带表注释和行数估算（如 ≈1.2M 行），表多时可传 `filter` 按业务关键词缩小范围；富查询失败自动回退纯名单，兼容系统目录有差异的库。
- **流式瞬态故障自动重试**：输出开始前发生的网关断连、5xx、连接重置等瞬态错误，按限流同款退避策略自动重试；重试耗尽或不可重试的流错误附中文根因诊断（阶段/已输出 token/耗时），原始错误落 logs 表便于事后归因。
- **SQL 执行历史审计**：Agent 执行的查询落 logs 表（category=sql）并记入 jsonl 工具段 meta，便于事后追溯。

## [0.1.7] - 2026-08-21

### 修复

- 知识库工具命名统一为 `*_okf_knowledge` 族（读/写工具由 `load_okf_block`/`write_okf_block` 更名）：模型按命名规律臆造工具名时不再被服务端整体拒绝（修复火山等严格校验端点的 `PromptError: UnknownToolCall` 直接报错），数据分析提示词同步加入工具名纪律。
- 更新包下载完成后不再自动重启安装，改由用户手动重启；检查更新菜单文案换行。

### 变更

- 数据分析 Agent 提示词强化：新增唯一真相原则、认知循环（建立认知→发现矛盾→解释矛盾→更新认知）与方法纪律。

## [0.1.6] - 2026-08-21

### 变更

- 流式输出期间实时渲染 markdown 排版：标题、加粗、列表、表格、代码块边输出边排版（上一版"输出结束后才排版"的临时方案废除）；代码块语法高亮在段落完成后自动补齐，流式期间界面保持流畅。

## [0.1.5] - 2026-08-21

### 修复

- 流式输出期间最大化/还原窗口布局错乱（根治）：带图表引用的历史消息在流式期间每 150ms 被整个销毁重建（markdown 解析 + 代码高亮全量重算），渲染债滚雪球导致窗口尺寸变化长时间无法生效；改为块级缓存复用，长对话流式期界面保持即时响应。流式中的尾部文本同时改为纯文本直出、结束后再做完整排版，长任务界面更流畅。

## [0.1.4] - 2026-08-20

### 修复

- 流式输出期间窗口最大化/还原布局错乱、界面交互整体失响应：根因是流式 delta 逐 token 触发消息列表与 markdown 全量重渲染，主线程被占满（实测页面求值排队 12 秒以上）；delta 改为按任务缓冲、每 150ms 定频冲刷，流式期窗口操作恢复即时响应（实测延迟降至 1ms），长任务界面同时更流畅。

## [0.1.3] - 2026-08-20

### 修复

- 对话流式输出期间最大化/还原窗口布局错位（右侧大片空白或内容被裁）：根因是流式渲染高峰期主线程被任务洪水占满，依赖 JS 事件的壳层尺寸校准整体失效、内联像素尺寸闩在旧值；改回纯 CSS 百分比尺寸，由渲染管线驱动，不依赖 JS 即可自愈。

## [0.1.2] - 2026-08-20

### 新增

- 数据分析场景新增联网搜索工具（克制使用）：查不到的实时公开信息可联网补全，不替代数据查询。

### 修复

- 对话过程中布局偶发错位：根容器尺寸卡在旧值导致右侧出现大片空白、窗口按钮位置偏移，改为双通道校准并防乱序写入。
- 最大化窗口时布局缩水：根容器/侧栏改用百分比尺寸，并按原生窗口尺寸校准兜底。
- 启动时深色闪屏与首页跳转。

### 变更

- 关于弹窗文案去技术化；对话时终端日志噪音降低。

## [0.1.1] - 2026-08-18

### 修复

- 窗口关闭按钮在存在流式任务时偶发失效：补齐 `core:window:allow-destroy` 权限、关闭拦截器加异常兜底（任何异常不再卡死关闭）、✕/菜单关闭自动降级 destroy。
- macOS 更新 manifest 生成：按构建架构精确选择签名文件，拒绝无架构后缀的中间产物（0.1.0 首发时 macOS 条目曾指向不存在的资产，已手工修正；0.1.1 起由流水线正确生成）。

## [0.1.0] - 2026-08-18

首个公开发行版本。

### 新增

- 对话式任务助手：联网搜索、知识问答、文案撰写，多 LLM 服务商接入（OpenAI / Anthropic / OpenAI 兼容）。
- 对话式数据分析：接入 PostgreSQL / Hologres / MySQL 等数据源，自动发现表、注册视图、执行 SQL、生成图表（趋势/对比/占比）。
- 工作区知识库（OKF）：git 版本化的表释义、业务概念与排障记录，新会话自动继承，支持跨知识内链。
- brand.json 品牌定制：应用名、logo、首页文案、助手身份改一份配置文件即可定制成自己的专属工作台。
- 应用内自动更新：启动后静默检查、后台下载新版本、就绪后一键重启安装（minisign 签名校验）。
- 数据分析环境自动探测：DuckLake 扩展已安装时启动即就绪，无需每次点击「启用」。

### 变更

- 品牌更名 Daw（中文名「寒鸦数据工作台」），全新寒鸦 logo。
- 数据目录由 `~/.aioa` 迁移至 `~/.daw`，首次启动自动迁移、历史数据完整保留。
- 系统提示词去除业务专属表述，助手身份随 brand.json 注入。

### 移除

- 内部业务系统示例与专有名词（连接名示例、机构名等）全部替换为通用示例。