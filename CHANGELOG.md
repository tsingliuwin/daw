# Changelog

本项目遵循 [Keep a Changelog](https://keepachangelog.com/zh-CN/1.1.0/) 格式，版本号遵循[语义化版本](https://semver.org/lang/zh-CN/)。

## [0.1.10] - 2026-09-01

### 新增

- **知识库可信写入体系**：写入回执与 git 提交信息带板块名与 ±行数统计，净删≥5 行当场警告核对；空内容拒绝、回显标题剥离、标题空白归一化匹配防近似重复板块、「整文件形态」板块写入直接拒绝——「工具报成功 ≠ 知识落了盘」的整类静默失败被堵死，知识库 git 仓库成为可审计的变更历史。
- **聊天界面：历史轮次过程折叠 + turn 用量统计**（借鉴 deepseek-harness）：完成的回答把思考/工具调用过程折叠为「已思考 X 秒 · N 次工具调用」一行，长对话 DOM 显著下降；每轮回答显示用量（token/耗时），随消息持久化。
- **语义映射纪律进系统提示词**：业务概念的语义翻译是 Agent 的本职——先给业务理解再给数字、穷尽探查才可以说「没有」、权限是协作（主动附权限申请清单）、维护业务全局模型（怎么赚钱/主链路/相互制约）。
- **交付纪律进系统提示词**：所问即所答（不擅自升级成看板）、默认报有效业务口径、关键数字注明来源表与口径、报数默认带业务结构——「所有只给数字不给数据逻辑的都是耍流氓」。
- **SQL 错误分类器细化**：`function xxx does not exist`（远端无此函数）与 `operator does not exist`（类型不匹配，如 text 列 = 数字字面量）不再误报为「表不存在」诱导排查表；表缺失错误按是否下推给对症修法（单段带点引号拆两段 / 下推内层带 schema）。
- **复盘脚本增强**：知识库体检（空标题按真损失/残桩分类）、git 审计（最近知识变更 + 未提交变更检测）、覆盖任意路径工作区的 okf；用户纠正候选词表第四次校准（没有看懂/看不懂/结论是什么）。

### 修复

- **write_okf_knowledge 静默吞正文（根修）**：模型回显同名标题与写入标题构成重复时，去重把标题连同其后正文一起吃掉却报成功——权威口径落成空标题的 P0 已修复，历史残桩由体检脚本识别清理。
- **okf git 提交静默失败**：骨架批量提交撞 `index.lock` 被 `let _` 吞掉、80+ 表骨架从未入库——进程内互斥锁串行化、失败进日志、存量一次性回填。
- **知识板块近似重复**：写入与读取的标题匹配统一做空白归一化，「2026 年 / 2026 年 」不再复制出仅空格差异的过时副本。
- 知识体检残桩判定区分容器标题（空标题后跟更深层级标题是合法结构，不再误报）。

### 变更

- 用户画像新增「数据 Agent 观点」板块：从历史对话全量整理产品设计者的做数哲学（唯一真相/语义映射/交付纪律/业务地基/协作方式，原话锚定）。
- 新增 `docs/DESIGN-PRINCIPLES.md`：数据 Agent 设计原则（唯一真相/语义映射/交付纪律/知识地基/越用越懂/商业模型全局视角），方法论沉淀为开源项目资产。

## [0.1.9] - 2026-08-27

### 新增

- **系统提示词体系重构（借鉴 deepseek-harness）**：静态纪律与动态事实分离——当前时间与知识库大纲改以 `<runtime_context>` 快照随用户消息下发，system prompt 跨会话字节稳定，provider 前缀缓存不再随知识写入被击穿；错误处置统一决策树（权限/不存在/存储层级错误终局，不重试不绕行不静默跳过）；防漂移测试（提示词提及的工具名必须真实存在、占位符无残留、OKF 类别枚举单一真相源）。
- **用户画像（users 类别激活）**：用户表达偏好/习惯/纠错即沉淀五板块画像（角色与关注点/沟通与交付偏好/常用口径与基准/已拍板决策/纠错记录），开场随大纲注入——Agent 越用越懂用户。
- **任务协议（playbooks 类别）**：把用户确认过的交付格式沉淀为协议（适用问题+数据源+交付结构+验证清单），大纲注入、命中即按协议交付，不再临场发挥。首条协议：两期同比对比表。
- **知识生命周期（status）**：`update_okf_metadata` 支持 status=active/superseded，大纲对作废条目标记「⚠️已作废」，新旧知识打架有完整治理链。
- **会话复盘工具 `tools/review_transcript.py`**：工具时间线/慢查询/错误/一致性体检/用户纠正候选一键输出，「用-查-改-用」循环的固定入口。

### 修复

- **下推守卫被换行符绕过（根修）**：pushdown 表守卫的表名提取用字面空格匹配，多行 SQL 的 `\nFROM` 全数漏检——一单 15 次 24~45s 全表拉取慢查询；改词边界解析后守卫全生效。
- **同窗不同数（P0-B 根修）**：timestamptz 朴素字面量的解释随会话时区变化（ICU 扩展加载触发 UTC→+08 翻转）且与下推路径（远端恒 +08）劈叉，同一时间窗两次查询差出边界时段订单；连接创建时钉死 `TimeZone='Asia/Shanghai'`，双路径数字验证一致，时间过滤统一直接北京墙钟。
- **reasoning 整段双写**：rig 流 `ReasoningDelta` 与完整 `Reasoning` 事件均按增量发送，思考段翻倍显示；完整事件改为前缀去重只补发余量。
- **SQL 禁词误伤 is_deleted**：`contains("DELETE")` 误拦合法列名；改词边界+字符串字面量抹除匹配。
- **COPY binary 错误剥壳**：postgres 扩展错误把根因埋在整段内层 SQL 之后，模型靠猜；剥壳后权限/表不存在/CAST 失败直指根因，错误文案含修复指引。
- **load_okf_knowledge 标题模糊匹配**：标题漏「（…）」尾巴时不再只回「未找到」——前缀/包含唯一命中直接返回，多候选列清单。
- 大纲徽标 `[pushdown]` → `[下推表·禁直查]`（曾致模型读反规则）；select 类小修复若干。

### 变更

- 工具描述与实现对齐（禁词表、行数截断、OKF 类别全枚举）；清理死代码（`get_system_preamble` 命令、未接线的 `SkillRegistry`）。
- SQL 审计 meta 增加 firstRow 结果指纹，事后可对账。

## [0.1.8] - 2026-08-27

### 新增

- **选表经验知识类别（selections/）**：数据分析 Agent 选表前先查阅已沉淀的「选表经验」——哪类业务问题用哪些表（首选表、交叉验证表、慎用/排除）。命中直接按经验选表、跳过探索；分析中确立的表选择偏好（用户纠正、验证过的映射）也会立即沉淀，下次同类问题不再重复探索。知识库大纲与提示词同步纳入该类别。
- **list_remote_tables 注释/行数/filter**：探查数据源时 postgres 表现在附带表注释和行数估算（如 ≈1.2M 行），表多时可传 `filter` 按业务关键词缩小范围；富查询失败自动回退纯名单，兼容系统目录有差异的库。
- **流式瞬态故障自动重试**：输出开始前发生的网关断连、5xx、连接重置等瞬态错误，按限流同款退避策略自动重试；重试耗尽或不可重试的流错误附中文根因诊断（阶段/已输出 token/耗时），原始错误落 logs 表便于事后归因。
- **SQL 执行历史审计**：Agent 执行的查询落 logs 表（category=sql）并记入 jsonl 工具段 meta，便于事后追溯。

## [0.1.7] - 2026-08-21

### 修复

- 知识库工具命名统一为 `*_okf_knowledge` 族（读/写工具由 `load_okf_block`/`write_okf_block` 更名）：模型按命名规律臆造工具名时不再被服务端整体拒绝（修复火山等严格校验端点的 `PromptError: UnknownToolCall` 直接报错），数据分析提示词同步加入工具名纪律。
- 更新包下载完成后不再自动重启安装，改由用户手动重启；检查更新菜单文案换行。

### 变更

- 数据分析 Agent 提示词强化：新增唯一真相原则、认知循环（建立认知→发现矛盾→解释矛盾→更新认知）与方法纪律。

## [0.1.6] - 2026-08-21

### 变更

- 流式输出期间实时渲染 markdown 排版：标题、加粗、列表、表格、代码块边输出边排版（上一版"输出结束后才排版"的临时方案废除）；代码块语法高亮在段落完成后自动补齐，流式期间界面保持流畅。

## [0.1.5] - 2026-08-21

### 修复

- 流式输出期间最大化/还原窗口布局错乱（根治）：带图表引用的历史消息在流式期间每 150ms 被整个销毁重建（markdown 解析 + 代码高亮全量重算），渲染债滚雪球导致窗口尺寸变化长时间无法生效；改为块级缓存复用，长对话流式期界面保持即时响应。流式中的尾部文本同时改为纯文本直出、结束后再做完整排版，长任务界面更流畅。

## [0.1.4] - 2026-08-20

### 修复

- 流式输出期间窗口最大化/还原布局错乱、界面交互整体失响应：根因是流式 delta 逐 token 触发消息列表与 markdown 全量重渲染，主线程被占满（实测页面求值排队 12 秒以上）；delta 改为按任务缓冲、每 150ms 定频冲刷，流式期窗口操作恢复即时响应（实测延迟降至 1ms），长任务界面同时更流畅。

## [0.1.3] - 2026-08-20

### 修复

- 对话流式输出期间最大化/还原窗口布局错位（右侧大片空白或内容被裁）：根因是流式渲染高峰期主线程被任务洪水占满，依赖 JS 事件的壳层尺寸校准整体失效、内联像素尺寸闩在旧值；改回纯 CSS 百分比尺寸，由渲染管线驱动，不依赖 JS 即可自愈。

## [0.1.2] - 2026-08-20

### 新增

- 数据分析场景新增联网搜索工具（克制使用）：查不到的实时公开信息可联网补全，不替代数据查询。

### 修复

- 对话过程中布局偶发错位：根容器尺寸卡在旧值导致右侧出现大片空白、窗口按钮位置偏移，改为双通道校准并防乱序写入。
- 最大化窗口时布局缩水：根容器/侧栏改用百分比尺寸，并按原生窗口尺寸校准兜底。
- 启动时深色闪屏与首页跳转。

### 变更

- 关于弹窗文案去技术化；对话时终端日志噪音降低。

## [0.1.1] - 2026-08-18

### 修复

- 窗口关闭按钮在存在流式任务时偶发失效：补齐 `core:window:allow-destroy` 权限、关闭拦截器加异常兜底（任何异常不再卡死关闭）、✕/菜单关闭自动降级 destroy。
- macOS 更新 manifest 生成：按构建架构精确选择签名文件，拒绝无架构后缀的中间产物（0.1.0 首发时 macOS 条目曾指向不存在的资产，已手工修正；0.1.1 起由流水线正确生成）。

## [0.1.0] - 2026-08-18

首个公开发行版本。

### 新增

- 对话式任务助手：联网搜索、知识问答、文案撰写，多 LLM 服务商接入（OpenAI / Anthropic / OpenAI 兼容）。
- 对话式数据分析：接入 PostgreSQL / Hologres / MySQL 等数据源，自动发现表、注册视图、执行 SQL、生成图表（趋势/对比/占比）。
- 工作区知识库（OKF）：git 版本化的表释义、业务概念与排障记录，新会话自动继承，支持跨知识内链。
- brand.json 品牌定制：应用名、logo、首页文案、助手身份改一份配置文件即可定制成自己的专属工作台。
- 应用内自动更新：启动后静默检查、后台下载新版本、就绪后一键重启安装（minisign 签名校验）。
- 数据分析环境自动探测：DuckLake 扩展已安装时启动即就绪，无需每次点击「启用」。

### 变更

- 品牌更名 Daw（中文名「寒鸦数据工作台」），全新寒鸦 logo。
- 数据目录由 `~/.aioa` 迁移至 `~/.daw`，首次启动自动迁移、历史数据完整保留。
- 系统提示词去除业务专属表述，助手身份随 brand.json 注入。

### 移除

- 内部业务系统示例与专有名词（连接名示例、机构名等）全部替换为通用示例。