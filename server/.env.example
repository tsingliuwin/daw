# AIOA 工作台服务端配置
# 复制此文件为 .env 并填入实际值。

# PostgreSQL 连接串（必须配置）
DATABASE_URL=postgresql://user:pass@localhost:5432/aioa

# JWT 签发密钥（随机长字符串，务必修改）
JWT_SECRET=change-me-to-a-random-secret

# 服务端口（本地开发用，云函数部署时由平台决定）
PORT=3000

# 企业配置（名称/LLM/搜索/用户）通过管理端在 PG 数据库里管理，
# 不再通过环境变量配置。
